<UserControl x:Class="BMTECHRD.Pos.App.Views.TablesMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BMTECHRD.Pos.App.Controls"
             xmlns:models="clr-namespace:BMTECHRD.Pos.App.Models"
             Opacity="0">
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid Background="#0B1220">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Margin="14" Background="#1F2937" CornerRadius="12" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <StackPanel>
                        <TextBlock Text="Mapa de mesas" Foreground="#F9FAFB" FontSize="24" FontWeight="SemiBold"/>
                        <TextBlock Text="Click abre comanda digital · arrastra para mover mesa" Foreground="#93C5FD"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="24,0,0,0" VerticalAlignment="Center">
                        <Border Background="#064E3B" CornerRadius="8" Padding="10,6" Margin="0,0,8,0">
                            <TextBlock Foreground="#A7F3D0" FontWeight="Bold">
                                <Run Text="Disponibles: "/><Run Text="{Binding AvailableTables}"/>
                            </TextBlock>
                        </Border>
                        <Border Background="#1E3A8A" CornerRadius="8" Padding="10,6">
                            <TextBlock Foreground="#BFDBFE" FontWeight="Bold">
                                <Run Text="Ocupadas: "/><Run Text="{Binding OpenTables}"/>
                            </TextBlock>
                        </Border>
                    </StackPanel>
                </StackPanel>

                <TextBlock Grid.Column="1" FontFamily="Segoe Fluent Icons" Text="&#xEC7A;" Foreground="#60A5FA" FontSize="26" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <Border Grid.Row="1" Margin="14,0,14,14" Background="#111827" CornerRadius="12">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Tables}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas Width="1800" Height="1100" IsItemsHost="True" />
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding PosX}"/>
                            <Setter Property="Canvas.Top" Value="{Binding PosY}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:TableModel}">
                            <controls:DraggableTableControl/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Border>
    </Grid>
</UserControl>
