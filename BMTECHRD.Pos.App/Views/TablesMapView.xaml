<UserControl x:Class="BMTECHRD.Pos.App.Views.TablesMapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:BMTECHRD.Pos.App.Controls"
             xmlns:viewmodels="clr-namespace:BMTECHRD.Pos.App.ViewModels"
             xmlns:models="clr-namespace:BMTECHRD.Pos.App.Models"
             >
    <UserControl.Resources>
        <Style TargetType="ScrollViewer">
            <Setter Property="HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="VerticalScrollBarVisibility" Value="Auto"/>
        </Style>
        <DropShadowEffect x:Key="DropShadowEffect" Color="#000" BlurRadius="8" ShadowDepth="2" Opacity="0.4"/>
    </UserControl.Resources>

    <Grid Background="#FF1F2022">
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding Tables}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas IsItemsHost="True" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style>
                        <Setter Property="Canvas.Left" Value="{Binding PosX}"/>
                        <Setter Property="Canvas.Top" Value="{Binding PosY}"/>
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="models:TableModel">
                        <controls:DraggableTableControl/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>