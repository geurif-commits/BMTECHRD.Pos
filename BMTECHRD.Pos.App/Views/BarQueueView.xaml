<UserControl x:Class="BMTECHRD.Pos.App.Views.BarQueueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:models="clr-namespace:BMTECHRD.Pos.App.Models"
             Height="Auto" Width="Auto">
    <Border Background="#0B1220" Padding="16">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="#1F2937" CornerRadius="12" Padding="12" Margin="0,0,0,12">
                <DockPanel>
                    <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                        <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE7C3;" Foreground="#38BDF8" FontSize="24" Margin="0,0,10,0"/>
                        <TextBlock Text="BAR" Foreground="White" FontSize="28" FontWeight="Bold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Content="Refrescar" x:Name="BtnRefresh" Width="140" Height="40" DockPanel.Dock="Right"/>
                </DockPanel>
            </Border>

            <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Left" Margin="0,0,0,12">
                <Button Content="Pendientes" x:Name="BtnPending" Width="140" Height="38" Margin="0,0,8,0"/>
                <Button Content="En proceso" x:Name="BtnInProgress" Width="140" Height="38" Margin="0,0,8,0"/>
                <Button Content="Todos" x:Name="BtnAll" Width="120" Height="38"/>
            </StackPanel>

            <ScrollViewer Grid.Row="2">
                <ListView x:Name="ItemsListView" ItemsSource="{Binding Items}" BorderThickness="0" Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:ProductionQueueItemModel}">
                            <Border Background="#1F2937" CornerRadius="10" Padding="12" Margin="0,0,0,12" RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <TranslateTransform Y="0"/>
                                </Border.RenderTransform>
                                <Border.Triggers>
                                    <EventTrigger RoutedEvent="Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)" From="6" To="0" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Border.Triggers>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="220"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center">
                                        <TextBlock Text="Mesa" Foreground="#A5B4FC" FontSize="12" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding TableNumber}" Foreground="White" FontSize="30" FontWeight="Bold" HorizontalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="12,0,0,0">
                                        <TextBlock Text="{Binding ProductName}" Foreground="White" FontSize="20" FontWeight="SemiBold"/>
                                        <TextBlock Foreground="#CBD5E1" FontSize="15">
                                            <Run Text="Cantidad: "/><Run Text="{Binding Quantity}"/>
                                        </TextBlock>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2" HorizontalAlignment="Right" VerticalAlignment="Center">
                                        <TextBlock Text="{Binding TimeLabel}" Foreground="#93C5FD" HorizontalAlignment="Right"/>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
                                            <Button Content="En proceso" x:Name="BtnStart" Width="100" Height="34" Margin="0,0,8,0"/>
                                            <Button Content="Listo" x:Name="BtnDone" Width="90" Height="34"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>
