<UserControl x:Class="BMTECHRD.Pos.App.Views.StartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:BMTECHRD.Pos.App.ViewModels"
             xmlns:models="clr-namespace:BMTECHRD.Pos.App.Models"
             Height="Auto" Width="Auto">
    <Border Background="#FF121213" Padding="24">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Orientation="Vertical" HorizontalAlignment="Center" VerticalAlignment="Top" Width="640">
                <TextBlock Text="Select Business" Foreground="White" FontSize="20" Margin="0,0,0,12"/>
                <ComboBox x:Name="CbBusinesses" ItemsSource="{Binding Businesses}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedBusiness}" Height="52" FontSize="18"/>

                <Image x:Name="ImgLogo" Height="120" Margin="0,12,0,12" Visibility="Collapsed"/>

                <StackPanel Orientation="Horizontal" Margin="0,4,0,8">
                    <TextBlock x:Name="TxtQuickHint" Foreground="#93C5FD" FontSize="14" TextWrapping="Wrap" Visibility="Collapsed" VerticalAlignment="Center"/>
                    <Button x:Name="BtnClearQuickLogin" Content="Usar otro usuario" Height="30" Margin="10,0,0,0" Visibility="Collapsed"/>
                </StackPanel>

                <TextBlock Text="Username (omit for quick login)" Foreground="White" Margin="0,4,0,2"/>
                <TextBox x:Name="TxtUsername" Height="52" FontSize="18" Margin="0,0,0,6"/>

                <TextBlock Text="PIN / Password (4-12)" Foreground="White" Margin="0,4,0,2"/>
                <PasswordBox x:Name="TxtPassword" Height="52" FontSize="18" MaxLength="12" Margin="0,0,0,6"/>

                <Button x:Name="BtnLogin" Content="Enter" Height="56" FontSize="20" Margin="0,12,0,0"/>
                <TextBlock x:Name="TxtError" Foreground="OrangeRed" FontSize="14" Margin="0,8,0,0"/>

            </StackPanel>
        </Grid>
    </Border>
</UserControl>