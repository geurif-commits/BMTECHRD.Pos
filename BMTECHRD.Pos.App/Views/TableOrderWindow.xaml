<Window x:Class="BMTECHRD.Pos.App.Views.TableOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:models="clr-namespace:BMTECHRD.Pos.App.Models"
        mc:Ignorable="d"
        Title="Comanda digital"
        WindowStartupLocation="CenterOwner"
        Width="1140"
        Height="740"
        Background="#0B1220"
        Opacity="0">
    <Window.Triggers>
        <EventTrigger RoutedEvent="Window.Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.25"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </Window.Triggers>

    <Window.Resources>
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2563EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1D4ED8"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#1E40AF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*"/>
            <ColumnDefinition Width="1.1*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" CornerRadius="16" Background="#1F2937" Padding="16" Margin="0,0,14,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <DockPanel LastChildFill="False">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="{Binding HeaderTitle}" FontSize="24" FontWeight="SemiBold" Foreground="#F9FAFB"/>
                        <TextBlock Text="Toque un producto para agregarlo al pedido." Foreground="#9CA3AF" Margin="0,2,0,0"/>
                    </StackPanel>
                    <TextBlock DockPanel.Dock="Right" FontFamily="Segoe Fluent Icons" Text="&#xE8A7;" FontSize="26" Foreground="#60A5FA" VerticalAlignment="Center"/>
                </DockPanel>

                <Grid Grid.Row="1" Margin="0,16,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.4*"/>
                        <ColumnDefinition Width="1*"/>
                    </Grid.ColumnDefinitions>

                    <TextBox Grid.Column="0" Height="38" VerticalContentAlignment="Center" Padding="10,0" BorderThickness="0" Background="#374151" Foreground="White" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}"/>

                    <ComboBox Grid.Column="1" Margin="10,0,0,0" Height="38" ItemsSource="{Binding Categories}" DisplayMemberPath="Name" SelectedItem="{Binding SelectedCategory}"/>
                </Grid>

                <ListBox Grid.Row="2" BorderThickness="0" Background="Transparent" ItemsSource="{Binding FilteredProducts}">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate><WrapPanel/></ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:MenuProductModel}">
                            <Border Width="220" Height="144" Margin="8" CornerRadius="12" Background="#374151" Padding="12" Cursor="Hand" RenderTransformOrigin="0.5,0.5">
                                <Border.RenderTransform>
                                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                                </Border.RenderTransform>
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter Property="Background" Value="#334155"/>
                                                <Trigger.EnterActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1.03" Duration="0:0:0.15"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1.03" Duration="0:0:0.15"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.EnterActions>
                                                <Trigger.ExitActions>
                                                    <BeginStoryboard>
                                                        <Storyboard>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX" To="1" Duration="0:0:0.12"/>
                                                            <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY" To="1" Duration="0:0:0.12"/>
                                                        </Storyboard>
                                                    </BeginStoryboard>
                                                </Trigger.ExitActions>
                                            </Trigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <Border.InputBindings>
                                    <MouseBinding MouseAction="LeftClick" Command="{Binding DataContext.AddProductCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                </Border.InputBindings>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock FontFamily="Segoe Fluent Icons" Text="&#xE719;" Foreground="#93C5FD" FontSize="22"/>
                                    <TextBlock Grid.Row="1" Margin="0,8,0,4" TextWrapping="Wrap" Text="{Binding Name}" Foreground="#F3F4F6" FontWeight="SemiBold" FontSize="16"/>
                                    <DockPanel Grid.Row="2">
                                        <TextBlock DockPanel.Dock="Left" Text="{Binding Area}" Foreground="#A7F3D0" FontSize="12" FontWeight="Bold"/>
                                        <TextBlock DockPanel.Dock="Right" Text="{Binding Price, StringFormat=C$ {0:N2}}" Foreground="#FBBF24" FontWeight="SemiBold"/>
                                    </DockPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <Border Grid.Column="1" CornerRadius="16" Background="#1F2937" Padding="14">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Pedido acumulado" FontSize="20" FontWeight="SemiBold" Foreground="#F9FAFB"/>

                <ListView Grid.Row="1" Margin="0,12,0,12" ItemsSource="{Binding Cart}" BorderThickness="0" Background="Transparent">
                    <ListView.ItemTemplate>
                        <DataTemplate DataType="{x:Type models:TableOrderLineModel}">
                            <Border Background="#374151" CornerRadius="10" Padding="10" Margin="0,0,0,8">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding ProductName}" Foreground="White" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Area}" Foreground="#9CA3AF" FontSize="12"/>
                                        <TextBlock Text="{Binding LineTotal, StringFormat=C$ {0:N2}}" Foreground="#FBBF24"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                                        <Button Content="-" Width="28" Height="28" Command="{Binding DataContext.DecreaseLineCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                        <TextBlock Text="{Binding Quantity}" Foreground="White" Margin="10,0" VerticalAlignment="Center"/>
                                        <Button Content="+" Width="28" Height="28" Command="{Binding DataContext.IncreaseLineCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding}"/>
                                    </StackPanel>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <DockPanel Grid.Row="2" Margin="0,0,0,10">
                    <TextBlock Text="Total" Foreground="#D1D5DB" FontSize="16"/>
                    <TextBlock Text="{Binding Total, StringFormat=C$ {0:N2}}" Foreground="#34D399" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right"/>
                </DockPanel>

                <Button Grid.Row="3" Height="46" Style="{StaticResource PrimaryButton}" Command="{Binding SendOrderCommand}" Content="Enviar comanda"/>
            </Grid>
        </Border>
    </Grid>
</Window>
